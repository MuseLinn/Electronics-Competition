---
type: status
updated: 2026-05-06
tags:
  - status
  - current
---

# 当前状态

## 今天的关键结论

AD9144/JESD204 standalone 工程已经生成 `top_direct.bit`，K325T FPGA 本体也通过了最小 ILA 诊断。FMCADDA-9250-9144 子卡插上后，等待 vendor reset/config 序列完成，TX/RX ILA clock 可用。

AD9144 TX 数字侧当前状态：QPLL lock、TX reset done、TX tready、SYNC、SYSREF 和 payload data 均已观察到正常活动。下一步是把示波器接到 DAC 输出端，先找约 50MHz 的 vendor ROM 正弦波。

AD9250 RX 侧当前 `tvalid=0`，后续单独排查，不阻塞先看 DAC 输出。

## 工程状态表

| 工程 | 状态 | 下一步 |
|---|---|---|
| LED/基础例程 | 已验证 | 作为烧录和约束参考 |
| DDS LED | bitstream 已生成 | 可做板级确认 |
| FPGA-only diagnostic | 已验证 | 证明 K325T/JTAG/100MHz/ILA 正常 |
| AD9144 standalone | bitstream 已生成，TX 数字链路已活 | 示波器测 OUT1，预期约 50MHz |
| AD9250 RX | reset/sysref 有活动但 `tvalid=0` | DAC 输出确认后单独排查 |
| `D:\awg_fpga` AD9144 integration | 暂停 | standalone DAC 输出确认后再合并 |
| DDR3/PCIe | 未启动 | 等 DAC 通路稳定后推进 |

## 现在能做的事

- 接示波器到 OUT1，使用 50 ohm 终端，寻找约 50MHz 输出。
- 如果 OUT1 平线，依次试 OUT2-OUT4。
- 保留 `tx_ila.csv` / `rx_ila.csv` 作为数字链路基线。
- 清理 timing 报告中的 reset/debug/CDC 路径。
- 准备把 standalone 成功路径合并回 `D:\awg_fpga` 主工程。

## 现在不建议做的事

- 不要继续把当前问题当作 license 阻塞处理。
- 不要在看到 DAC 模拟输出前推进 DDR3/PCIe。
- 不要把 AD9250 RX `tvalid=0` 当成 DAC 输出首测的阻塞项。
