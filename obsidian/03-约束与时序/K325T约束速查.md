---
type: constraints
updated: 2026-05-06
tags:
  - xdc
  - k325t
  - constraints
---

# K325T 约束速查

## 原则

- 不要整文件包含 `K7_IO.xdc`。
- 只把当前顶层端口需要的引脚复制进工程约束。
- 每个顶层 IO 必须同时有 `PACKAGE_PIN` 和 `IOSTANDARD`。
- 差分时钟必须配合 RTL 中的 `IBUFDS` 使用。

## 查询入口

| 用途 | 文件 |
|---|---|
| 快速查 IO | `D:\FPGA\Kintex7\Kintex7\3_开发板原理图\K7_IO.xdc` |
| 表格查 IO | `D:\FPGA\Kintex7\Kintex7\3_开发板原理图\K7开发板IO引脚分配表.xlsx` |
| 电气确认 | `D:\FPGA\Kintex7\Kintex7\3_开发板原理图\K7_BASE_1V3_2025_0111_USER.pdf` |
| 核心板确认 | `D:\FPGA\Kintex7\Kintex7\3_开发板原理图\K7_CORE_V1.0.pdf` |

## 基础约束模板

```tcl
set_property PACKAGE_PIN AE10 [get_ports sys_clk_p]
set_property IOSTANDARD DIFF_SSTL15_DCI [get_ports sys_clk_p]
set_property PACKAGE_PIN AF10 [get_ports sys_clk_n]
set_property IOSTANDARD DIFF_SSTL15_DCI [get_ports sys_clk_n]
create_clock -period 10.000 -name sys_clk [get_ports sys_clk_p]

set_property PACKAGE_PIN AB25 [get_ports sys_rst_n]
set_property IOSTANDARD LVCMOS33 [get_ports sys_rst_n]

set_property PACKAGE_PIN R24 [get_ports {led[0]}]
set_property IOSTANDARD LVCMOS33 [get_ports {led[0]}]
set_property PACKAGE_PIN R23 [get_ports {led[1]}]
set_property IOSTANDARD LVCMOS33 [get_ports {led[1]}]
```

## 常见 DRC

| DRC | 含义 | 处理 |
|---|---|---|
| NSTD-1 | 端口没有 IOSTANDARD | 为所有顶层端口补 `IOSTANDARD` |
| UCIO-1 | 端口没有 PACKAGE_PIN | 为所有顶层端口补 `PACKAGE_PIN` |
| no_clock | 寄存器没有被约束时钟驱动 | 检查 create_clock/generated clock/CDC |

## AD9144 standalone XDC

当前 K325T-specific XDC：

```text
D:\FPGA\ad9144_bringup_k325t\constraints\top_k325t_fmc.xdc
```

不要使用 vendor demo 自带 `top.xdc`，它对应其他板卡/GT bank。

