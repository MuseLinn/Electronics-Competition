---
type: architecture
updated: 2026-05-06
tags:
  - architecture
  - awg
  - jesd204
---

# 顶层架构图

## 最终目标架构

```mermaid
flowchart LR
    PC["上位机/控制界面"] --> PCIe["PCIe XDMA"]
    PCIe --> REG["寄存器控制面"]
    PCIe --> DDR["DDR3 波形缓存"]
    REG --> CTRL["波形/扫频/幅度控制"]
    DDR --> MUX["波形数据选择"]
    DDS["DDS/NCO"] --> MUX
    MUX --> SCALE["幅度/偏置/限幅"]
    SCALE --> JESD["JESD204B TX"]
    LMK["LMK04828 时钟/SYSREF"] --> JESD
    JESD --> GTX["GTXE2 Bank 117"]
    GTX --> DAC["AD9144 FMC DAC"]
    DAC --> SCOPE["示波器/负载"]
```

## 当前可执行架构

当前先使用 vendor demo 做 AD9144 standalone bring-up，不急着把它并入 `D:\awg_fpga` 主工程。

```mermaid
flowchart TD
    SRC["Vendor K7 FMCADDA demo"] --> XDC["K325T FMC XDC"]
    XDC --> BUILD["build_direct.tcl"]
    BUILD --> SYN["synth/opt/place/route 已通过"]
    SYN --> BIT{"write_bitstream"}
    BIT -- "缺 JESD204 IP license" --> BLOCK["当前阻塞"]
    BIT -- "license 解决后" --> HW["Hardware Manager + ILA/VIO"]
    HW --> SCOPE["链路稳定后再看示波器"]
```

## 为什么不先改主工程

`D:\awg_fpga\rtl\top\awg_fmc_adda_top.v` 目前仍是骨架，尚未真正连接 JESD204 PHY。直接在主工程里改容易同时引入顶层、约束、IP、时序、硬件多类问题。正确路线是：

1. 先用 `D:\FPGA\ad9144_bringup_k325t` 证明 vendor demo 可以在 K325T 上建链。
2. 再把已验证的 XDC、reset、JESD status、数据入口迁入 `D:\awg_fpga`。
3. 最后替换 vendor ROM 数据为 AWG DDS/BRAM/DDR 数据。

## 关联笔记

- [[AD9144 Bring-Up]]
- [[JESD204数据链路]]
- [[开发路线图]]
- [[时序与CDC风险]]

